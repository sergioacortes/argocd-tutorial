apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dockerwalkthroughprivate-dev
  namespace: argocd
spec:
  project: default
  source:
    repoURL: ghcr.io/sergioacortes/charts
    chart: dockerwalkthroughprivate
    targetRevision: 0.2.0-rev.1
    helm:
      releaseName: dockerwalkthroughprivate
      values: |
        namespace: dockerwalkthroughprivate-dev
        service:
          type: NodePort
        image:
          repository: ghcr.io/sergioacortes/docker-walkthrough-private/client
          tag: 0.2.0-rev.1
        imagePullSecrets:
          - name: ghcr-credentials
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
